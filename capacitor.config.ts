import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'AS3 Assignment 2 MWE',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
